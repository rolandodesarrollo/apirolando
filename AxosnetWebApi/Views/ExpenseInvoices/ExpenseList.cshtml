
@{
    ViewBag.Title = "ExpenseList";
}

<script src="~/Scripts/Controllers/ExpensesCtrl.js"></script>
<div ng-controller="expensesListCtrl">
    <br />
    <br />
    <div class="row">
        <div class="col-sm-6">
            <h3>{{message}}</h3>
            <h4>{{providers.length}} registrados</h4>
        </div>
        <div class="col-sm-6">
            <p><a href="#" ng-click="openAddProviderPopUp()" class="btn btn-success btn-sm pull-right">Agregar</a></p>
        </div>
    </div>

</div>

